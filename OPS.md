# TransFin — операційна частина (приватно)

**Оновлено:** 25.09.2026 (Київ): стан сервера, SSH, fail2ban, бекапи, нумерація плану.
**Що тут:** усе, що стосується конкретних машин, доступів, секретів, тимчасових заходів та інцидентів. Знання про код — у `HANDOFF.md` репозиторію `transfin-backend`.
**Правило для цього файла:** **значень секретів тут немає й не буде** — лише де вони лежать, хто їх читає і як їх змінити.

---

## 1. Сервер (продакшн)

- **Адреса:** https://transfin.uk. DNS — Cloudflare (DNS-only).
- **Хостинг:** Hetzner CX23, `46.224.18.229`, Ubuntu 26.04. RAM 3,7 GiB, **swap немає**.
- **Вхід:** лише SSH за ключем, `root@46.224.18.229`, ключ `~/.ssh/id_ed25519` на Mac. Вхід за паролем вимкнено (`/etc/ssh/sshd_config.d/10-transfin-hardening.conf`; перевірено 25.09 `sshd -T`: `passwordauthentication no`, `kbdinteractiveauthentication no`, `permitrootlogin prohibit-password`).
- **Застосунок:** `transfin.service` (systemd, користувач `transfin`, `127.0.0.1:8000`, 1 worker); код у `/home/transfin/backend`, Python 3.12 через uv (`/opt/uv/python`). У unit-файлі `QUEUE_AUTOSTART_IN_WEB=false`, `QUEUE_POLLING_ENABLED=false` — сайт на сервері eCherha **не** читає.
- **e-TOLL recorder:** `transfin-etoll-recorder.service` (з `deploy/`), користувач `transfin`, логін бази `etoll_recorder`, `MemoryMax=256M`. Стан на 21.09: `active` + `enabled`.
- **Секрети:** `/etc/transfin/transfin.env` (`root:transfin`, `0640`). Ключі: `DATABASE_URL`, `LEDGER_DATABASE_URL`, `SESSION_SECRET_KEY` (64 символи), `SESSION_COOKIE_SECURE=true`, `LONTEX_ATLAS_BASE_URL/LOGIN/PASSWORD`, `ETOLL_DATABASE_URL`, `ALLOWED_HOSTS=transfin.uk,www.transfin.uk` (з 21.09). **Ніколи не друкувати.** Перед запуском коду застосунку на сервері завантажувати: `sudo -u transfin bash -c 'set -a; . /etc/transfin/transfin.env; set +a; …'` (значення не потрапляють в argv).
- **nginx + Let's Encrypt:** `/etc/nginx/sites-available/transfin`; http і www → https apex; невідомий Host → 444 або відмова TLS; `/docs`, `/redoc`, `/openapi.json` → 404; обмеження частоти на логін. Застосунок сам перевіряє Host (`ALLOWED_HOSTS`, з 21.09): **локальні перевірки лише з `-H 'Host: transfin.uk'`**, інакше 400. На сервері ніщо не звертається до `127.0.0.1:8000` напряму (перевірено 21.09: таймери, cron, `atq`, скрипти, агентів моніторингу немає).
- **PostgreSQL 18:**

  | База / роль | Для чого |
  |---|---|
  | `transfin_backend` (власник `transfin_backend`) | основна база сайту; схема `etoll` (власник `etoll_owner`) |
  | `transfin` (власник `transfin`) | Ledger; alembic з 21.09 — `0006_audit_actor` |
  | `transfin_worker` | воркер eCherha: лише DML, default privileges на нові таблиці; **без `UPDATE` на `crossing_log`** (тимчасово, §4) |
  | `etoll_owner` (NOLOGIN) | власник усього в `etoll`; члени з `INHERIT FALSE, SET TRUE`: `transfin_backend`, `etoll_recorder` |
  | `etoll_recorder` (LOGIN, SCRAM) | запис позицій; лише DML |

  CONNECT/TEMP для PUBLIC на `transfin` і `postgres` забрано.
- **Бекапи:** `transfin-pgdump.timer` щодня ~03:15 UTC → `/var/backups/transfin/{transfin_backend,transfin,globals}/`, 7 копій, скрипт `/usr/local/sbin/transfin-pgdump.sh` сам перевіряє дамп (`pg_restore --list`). **Копій поза сервером (off-site) немає.** Відкладені копії: перед міграцією e-TOLL — `/var/backups/transfin/pre-etoll-transfin_backend_20260920T173502Z.dump`; перед P1–P8 — `pre-p1p8-transfin_backend_20260921T080917Z.dump` і `pre-p1p8-transfin_20260921T080917Z.dump`. **Права теки (перевірено 25.09): `/var/backups/transfin` і її підтеки — `root:root 700`, файли `root 600`.** Нічний `transfin-pgdump.sh` працює від root: сам ставить ці права (`install -d -m 700 -o root -g root`, `umask 077`) і запускає `runuser -u postgres -- pg_dump … > файл` — дамп іде в stdout, файл створює root. Тому зміна теки на `root:postgres 0730` (22.09) кожної ночі скасовується; `pg_dump -f` від `postgres` у цю теку дає `Permission denied`. **Ручний дамп — так само через stdout від root** (рецепт у §9). Теку не змінюємо (власник, 25.09). Для `pg_restore` від `postgres` файл треба спершу скопіювати в теку, яку `postgres` може читати.
- **Користувач тунелю `transfin-tunnel`:** без shell і пароля; ключ дозволяє лише перенаправлення на `127.0.0.1:5432`; `/etc/ssh/sshd_config.d/20-transfin-tunnel.conf`.
- **Alembic:** жоден ланцюжок не будує схему з порожньої бази; сервер зібрано через `create_all` + `alembic stamp head` 16.09.

### Розгортання

```bash
cd /home/transfin/backend
sudo -u transfin git status --porcelain        # має бути порожньо
sudo -u transfin git fetch origin ui/journal-redesign
sudo -u transfin git log --oneline HEAD..origin/ui/journal-redesign
sudo -u transfin git merge --ff-only origin/ui/journal-redesign
systemctl restart transfin.service
```

Якщо є міграції: спершу дамп (`systemctl start transfin-pgdump.service` + відкладена копія), репетиція на відновленій копії з `upgrade → downgrade → upgrade`, лише потім справжня база, з `PGOPTIONS="-c lock_timeout=5s -c statement_timeout=60s"`. Після: `systemctl is-active`, `https://transfin.uk` → 200, `/api/...` без входу → 401, `journalctl -u transfin.service` без помилок. Для recorder — ще й `journalctl -u transfin-etoll-recorder --since "2026-09-20 17:46" | grep -c password=` → 0 (без `--since` виходить 7: старі рядки інциденту 20.09, §5 — журнал свідомо не чистили).

**Downgrade `e7011a5c3b19` видаляє схему `etoll` з даними — лише з окремого явного дозволу власника.**

---

## 2. Що на сервері зараз (22.09)

- **e-TOLL етап 2 розгорнуто 22.09 (~08:05 Київ):** код `d30cb9d`, міграція `c3e9a1f7d204` (8 таблиць у `etoll`), `cryptography==43.0.3` у venv (uv), ключ `ETOLL_SECRET_KEY` у `transfin.env` (згенеровано на сервері, не друкувався; копія файлу до зміни — `transfin.env.bak-etoll2-20260922T050400Z`). Служба `transfin-etoll-stage2` (`enabled`, `active`) — читає кабінет IKK раз на 2 год і листи e-TOLL, **лише для облікових записів, введених у картці компанії**. **Втрата ключа = повторне введення паролів IKK у картках.**

- **На сервері:** код `2f91a23` (P1–P8, P5a, перенесення журналу, виправлення зависання й логіста, FINANCE читає Журнал, роль VIEWER, ланцюжок «черга → перетин → журнал» — 21.09; C1 для архіву й автоматичні рейси з перетинів — 22.09, §6). Сайт `active`, recorder `active`/`enabled` (з 20.09 не перезапускався, його код не змінювався). Ledger — `0006_audit_actor`. Нерозгорнутого немає.
- **Облікові записи сайту:** активний лише id 1 (власник; ролі **LOGISTICIAN+SUPER_ADMIN** з 21.09 — як на Mac, інакше картка відрізка не бачила його у списку логістів). id 2 (ADMIN+LOGISTICIAN) неактивний з 17.09 ~12:26 UTC — див. §6.
- **Журнал ведеться лише тут (з 21.09):** перенесено з Mac 28 рейсів, 16 відрізків, 12 маршрутів (25 точок), 11 локацій, 14 контрагентів, 2 вкладення; 27 перетинів прив'язано до рейсів (12 наявних + 15 доданих). На сторінці видно 26 рейсів: 2 закриті без відрізків і перетинів (id 2, 19) сторінка ховає як «порожні оболонки» — так само було на Mac з 13.09.
- **Вкладення відрізків:** `/home/transfin/backend/storage/received_attachments/` (`transfin`, `750`, файли `640`). **У нічний `pg_dump` не входять** (§8).
- **Колега** (personnel id 3, створена власником 21.09): ролі **FINANCE + VIEWER** (з 21.09 ~23:30 Київ), доступ лише до **ТОВ «ВОТРАНС»**. Бачить усі сторінки (Черга, Довідники, Журнал, Перетини, Фінанси) у межах ВОТРАНС; змінювати може лише «Фінанси» (FINANCE), решта — лише перегляд, зміни — 403. Про водіїв бачить ім'я, компанію, дату найму й дати дії документів. Закрито: персонал і облікові записи, модуль документів, Delegowanie, документи рейсів, бекапи, e-TOLL. Рейси без тягача (3 з 6 ВОТРАНС на 21.09) їй не видно, доки тягач не вказано.
- **Архів eЧерги за розкладом:** 10:30 і 16:00 за Києвом, обидві компанії (з 21.09). Пропущене читання (Mac уночі вимкнений) наздоганяється одразу після старту воркера, але не частіше за розклад. Деталі — до 3 спроб, не частіше ніж раз на 60 хв (`QUEUE_ARCHIVE_DETAIL_MAX_ATTEMPTS`, `QUEUE_ARCHIVE_DETAIL_RETRY_MINUTES`).
- **Рейси з перетинів — автоматично (з 22.09):** перетин eЧерги з тягачем із довідника дає чернетку рейсу; перемикач `QUEUE_JOURNAL_AUTO_TRIPS` у `worker.env` Mac (зараз `true`; вимкнути — `false` і перезапуск воркера). Відкритий рейс без перетину ніколи не прив'язується сам: внутрішній (усі точки UA) не кандидат, інший — позначка «можливо, належить до рейсу №N» і вибір людини («Прив'язати до №N» / «Створити новий рейс»). Попередній рейс лише позначається «знову виїхав», не закривається. Кнопка «＋ Створити рейс» лишається. Що зробили б правила з перетинами без рейсу — `python -m app.journal_auto_trips` (лише читає); `--apply` — зробити.
- **15 старих порожніх чернеток** (id 1, 6, 10, 11, 13–18, 20, 22, 24, 27, 28; створені автоматикою до 15.09, 0 відрізків) — не чіпати, рішення власника (§7). У 11, 14, 15, 16 немає власного `truck_id` (лише через перетин).

- **e-TOLL етап 2 (кабінет IKK), стан 22.09:**
  - на сервері **Chromium 130** для `transfin` (`~/.cache/ms-playwright/chromium-1140`), headless працює;
  - дані входу IKK — лише в картці компанії (пароль зашифрований у базі); тимчасові `/etc/transfin/etoll_ikk.env` і `/root/etoll_ikk_credentials.sh` видалено (перевірено 25.09: обох файлів немає);
  - пробний вхід із сервера пройшов для ВОТРАНС і Транс Левел; тимчасові файли розвідки на сервері видалено;
  - автоматичне читання кабінету через внутрішній шлюз **не робимо** (рішення власника); далі — план варіанта 1 (сторінки як людина), чекає «так»;
  - старі файли e-TOLL з iCloud — у Кошику Mac (`~/.Trash/etoll-old-20260922`), очистити — власник;
  - паролі IKK власник вирішив не змінювати (22.09).
- **AC5801EP (Транс Левел):** в IKK статус «Active» **без пристрою** (0 OBE), верифікація «Positive», зареєстрований з 25.09.2023; проїздів в IKK щонайменше 90 днів немає (довший період IKK не віддає). У нас — **в архіві** (`trucks.active = false`, причина «Відновлено як заархівовано (перевідновлення бази)»), без `lontex_id`, без пристрою Tekson і без перетинів eЧерги. Вирішити власнику: деактивувати в IKK чи повернути в роботу.

---

## 3. Воркер eCherha на Mac (варіант B')

eCherha захищена від ботів (headless блокується, вхід — людина й капча), тому воркер працює на Mac власника у видимому згорнутому Chrome і пише в серверну базу через SSH-тунель.

- **Тунель:** LaunchAgent `com.transfin.db-tunnel`, ssh `127.0.0.1:15432` → сервер `127.0.0.1:5432`, користувач `transfin-tunnel`, ключ `~/.ssh/transfin_tunnel_ed25519`, окремий `~/.config/transfin/known_hosts`. У plist уже є `ServerAliveInterval=15`, `ServerAliveCountMax=3`, `ExitOnForwardFailure`, `KeepAlive`, `ThrottleInterval=60`.
- **Воркер (з 25.09 — окрема тека):** LaunchAgent `com.transfin.queue-worker` → `~/Projects/backend-worker/scripts/queue_worker_launchagent/run_worker.sh` → `python3.12 -m app.queue_worker` з **`~/Projects/backend-worker`** — git worktree репозиторію на локальній гілці `worker-live`, що стежить за `origin/ui/journal-redesign`. Перемикання гілок у `~/Projects/backend` воркер не чіпає. **У цій теці сесії Claude Code не працюють** (AGENTS §15).
- **Оновлення воркера — лише свідомо, з «так» власника:**
  ```bash
  git -C ~/Projects/backend-worker status --short --branch   # має бути чисто
  git -C ~/Projects/backend-worker fetch -q origin
  git -C ~/Projects/backend-worker log --oneline HEAD..origin/ui/journal-redesign
  git -C ~/Projects/backend-worker pull --ff-only
  launchctl kickstart -k gui/$(id -u)/com.transfin.queue-worker
  ```
  Далі — перший цикл у лозі (`Active Queue polling succeeded … errors=0`). Відкат: `git -C ~/Projects/backend-worker checkout --detach <попередній коміт>` + `kickstart -k`, потім `git -C ~/Projects/backend-worker checkout worker-live`.
- **Зміна plist:** лише через `install.sh` **з теки воркера** (`~/Projects/backend-worker/scripts/queue_worker_launchagent/install.sh --server 46.224.18.229`, без `--load` пише лише plist). Щоб launchd перечитав plist: `launchctl bootout …/com.transfin.queue-worker`, **дочекатись, поки процес вийде** (`pgrep -f app.queue_worker` порожній — вихід займає кілька секунд, інакше `bootstrap` дає помилку 5), потім `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.transfin.queue-worker.plist`. Копії plist до переїзду — `~/TransFin-private/launchagents-before-worker-folder-202609250820/`. Відкат переїзду: `install.sh` з `~/Projects/backend` + ті самі bootout/bootstrap.
- **Приватні файли (0600, поза git):** `~/.config/transfin/worker.env` (`DATABASE_URL` без пароля + перемикачі: `QUEUE_DOCUMENT_REMINDERS_ENABLED=false`, `QUEUE_TELEGRAM_ENABLED=false`, `QUEUE_DRIVER_DETAILS_ENABLED` не задано = вимкнено, `QUEUE_JOURNAL_AUTO_TRIPS=true` з 22.09; копія до зміни — `worker.env.bak-20260922`), `~/.config/transfin/pgpass` (пароль `transfin_worker`), `~/.config/transfin/known_hosts`. Шаблони plist і `install.sh`/`uninstall.sh` — `scripts/queue_worker_launchagent/` у репозиторії.
- **Профілі Chrome:** `~/Library/Application Support/TransFin/echerha_profiles/{1,2,3}` (~186 МБ). Сесії живуть тут і на інший комп'ютер не переносяться. Кеш сторінок: `~/Library/Caches/TransFin/echerha_profiles` (803 МБ; видаляти лише при зупиненому воркері).
- **Облікові записи eCherha** (id на сервері й Mac однакові):

  | id | Логін | Компанія |
  |---|---|---|
  | 1 | trans1321@icloud.com | ТОВ «ВОТРАНС» |
  | 2 | trans1321@gmail.com | ПП «Транс Левел» |
  | 3 | mv17111988mv@gmail.com | ПП «Транс Левел» |

- **Керування:**
  ```bash
  launchctl print gui/$(id -u)/com.transfin.queue-worker
  launchctl kickstart -k gui/$(id -u)/com.transfin.queue-worker
  launchctl bootout gui/$(id -u)/com.transfin.queue-worker
  launchctl disable gui/$(id -u)/com.transfin.queue-worker
  launchctl enable gui/$(id -u)/com.transfin.queue-worker
  launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.transfin.queue-worker.plist
  ```
  Те саме для `com.transfin.db-tunnel`. Зупиняти воркер, потім тунель; запускати навпаки.
- **Логи:** `~/Library/Logs/TransFin/queue_worker.err.log` (росте ~10 МБ/добу, ротації немає), `~/Library/Logs/TransFin/db_tunnel.err.log`. Корисні рядки: `Queue worker starting: … driver_details=False`; `Active Queue polling succeeded: checked=… saved=… errors=…`; `Active Queue cycle took …s` (норма ~140–155 с на 3 акаунти); `echerha account result account_id=… status=…`; `… database unavailable (…)` — короткий обрив тунелю.
- **Відомо й прийнято:** кожен перезапуск воркера — три короткі спалахи вікон (по Chrome на акаунт).
- **На сервері:** хто тримає lock — `pg_locks` + `pg_stat_activity` (`transfin-queue-worker@<Mac>:<pid>`); стан акаунтів — `company_external_accounts` (`last_read_status`, `last_success_at`, `login_request_status`).
- **Живлення:** від мережі `sleep 0`; **від батареї `sleep 5`** — Mac засинає, тунель рветься, воркер виходить і перезапускається (три нові вікна Chrome). Воркер надійний лише під живленням. 21.09 зарядка кілька годин не йшла, хоча власник підключив кабель (`pmset` показував Battery Power) — перевірити кабель/адаптер.

### Mac-сайт і архів його бази (21.09)

- **Mac-сайт зупинено** 21.09 (перед M0 перенесення журналу); ним більше не користуємось. Код сайту відмовляється стартувати на SQLite без `ALLOW_LOCAL_SQLITE=1`.
- **Архів:** `~/Projects/backend/transfin_backend.ARCHIVE-20260921.db` (`444`) — стан бази Mac на момент M0, sha256 `123c23b5…90d977`. Той самий знімок — `~/TransFin-private/journal-migration-20260921/mac_journal_source.db` (`400`). Фінанси Mac (Ledger, стара модель) **не переносимо**; дані з архіву не видаляємо.
- `transfin_backend.db` у теці репозиторію **більше немає** (§4 — забруднена копія під іншим іменем).

### Mac: диск і пам'ять (20–21.09)

- 72 «клони підпису коду» Chrome (`/private/var/folders/…/X/com.google.Chrome.code_sign_clone`) видалено скриптом `~/transfin_chrome_cleanup.sh`: звільнили **лише ~2 ГіБ** (`du` показував 100 — спільні блоки APFS). Автоприбирання (`--install-agent`) — гігієна, не потреба.
- Решту місця займають системні знімки `com.apple.os.update-*` та захищені теки — не чіпати; картина в «Сховищі». Великі теки, які власник вирішує сам: `~/Library/Application Support/Claude` (12 ГіБ), `Google/Chrome/OptGuideOnDeviceModel` (4 ГіБ), сміття тестів `/T/pytest-of-andreverum` (2 ГіБ).
- Власник видалив `~/Library/Caches/ms-playwright` — **воркеру це не шкодить**: усі точки запуску браузера використовують `channel="chrome"` (системний Chrome). Браузер Playwright знадобився б лише для запуску без цього параметра — тоді `playwright install chromium`.
- 21.09 Mac був у **важкому свопі** (7,5 з 9,2 ГБ, 5,36 млн вивантажень): повні прогони тестів тривали 40+ хв замість 6. Причина — Chrome (три вікна воркера + браузер власника), Claude, Telegram.

---

## 4. Тимчасові заходи

- ~~`REVOKE UPDATE ON public.crossing_log FROM transfin_worker`~~ (19.09) — **знято 21.09 ~23:25 Київ** (`GRANT UPDATE`) разом із лімітом спроб (`ceb8991`, воркер перезапущено перед GRANT). C1 для сторінок архіву (B) зроблено 22.09 (`a0d53ee`, §6; перевірено 25.09: коміт у HEAD і на сервері, `reuse_page` у `echerha_archive_ingestion.py`); деталі архіву наживо ще не перевірені. 11 старих перетинів (07–17.09) без деталей: eCherha їх уже не знаходить (`archive_detail_anchor_not_found`), після 3-ї спроби лишаються без деталей.
- **Копії `transfin.env` на сервері** (усі `root:transfin 640`, містять діючі на той момент секрети), **видалити після 27.09.2026**:

  | Копія | Навіщо |
  |---|---|
  | `transfin.env.bak-tekson-20260920T144808Z` | перед першим записом доступу Tekson |
  | `transfin.env.bak-etoll-20260920T174436Z` | перед записом `ETOLL_DATABASE_URL` |
  | `transfin.env.bak-tekson-20260920T181107Z` | перед записом перевипущеного доступу Tekson |

  Команда: `rm -f /etc/transfin/transfin.env.bak-*`.
- **Забруднена тестами база Mac** `~/Projects/backend/transfin_backend.TEST-POLLUTED-20260921.db` (`444`): колишня жива база Mac-сайту, у яку 21.09 частковий запуск тестів записав тестові рядки (§5). Не архів — архів окремо (§3). **Видалити після 27.09.2026 разом із копіями `transfin.env`** (рішення власника).
- **Відкладені дампи 21.09** на сервері: `pre-p1p8-*_20260921T080917Z` і `pre-journal-*_20260921T091318Z` у `/var/backups/transfin/` — до певності, що P1–P8 і перенесення журналу стабільні.

---

## 5. Інциденти

- **19.09 — паролі IKK e-TOLL** обох компаній лежали відкритим текстом у старому `etoll.py` (iCloud) і потрапили у вивід інструментів сесії: мій фільтр маскування не впізнав ключі в лапках. **Паролі змінити** (чекає на власника). Урок: кожен фільтр маскування спершу перевіряти на фейковому рядку.
- **20.09 — доступ Tekson у журналі сервера.** `httpx` за 17 с записав 7 рядків із повним URL (логін і пароль) у journald; ті самі значення — у вивід сесії. Сервіс зупинено, доступ **перевипущено власником 20.09**, у коді — інваріант 12 (приглушення сторонніх логерів + фільтр + тест). Журнал **не чистили** (зачистка стерла б усю історію, значення все одно змінене). Після запуску на виправленому коді: `password=` у журналі — 0.
- **20.09 — код доступу Tekson у чаті.** «Очищений» URL показував логін у шляху; виправлено (`redact_url` маскує середину шляху). Пароль не засвітився; перевипуск не знадобився.
- **21.09 — тести ходили в ПриватБанк.** Тест-обхід маршрутів із P1 на кожному повному прогоні (≈10 разів за 20–21.09) робив справжній запит на виписки з `~/.privatbank_credentials.json`. Лише читання, нічого не записано. Виправлено в `91fd334`; глобальна заборона мережі в тестах — у беклозі.
- **21.09 — права файла ключів ПриватБанку:** `~/.privatbank_credentials.json` мав **`644`**; власник виправив на `600` того ж дня.
- **21.09 — сайт висів після перенесення журналу** (~09:15–09:37 UTC, двічі). Сторінка Журналу просила відрізки кожного з 26 рейсів окремим запитом одночасно; пул з'єднань (15) вичерпався, а перевірка входу чекала з'єднання на циклі подій — висло все, `/health` теж. Власник двічі перезапускав сайт. Виправлено в `bd6e6f1` (перевірка входу — у потоках, відрізки — одним запитом); після розгортання — максимум 5 з'єднань, без зависань.
- **21.09 — картка відрізка мовчки стерла логіста** у відрізках 18 і 21: власника (id 1) ще не було в списку логістів (лише SUPER_ADMIN), поле показувалось порожнім і збереження писало `null`. Відновлено з архіву Mac (`logistician_id = 1`, ім'я — зі знімка), власнику додано роль LOGISTICIAN; у коді (`29a9895`) поле зберігає значення поза списком як «(не в списку)».
- **21.09 — тести записали в живу базу Mac.** Частковий запуск тестів (без модуля, що випадково задавав тимчасову базу) пішов на типову `transfin_backend.db` у репозиторії: +2 компанії, +3 тягачі, +3 рейси, +3 відрізки, +7 людей і облікових записів (усе тестове), змінено вміст `queue_snapshots`. Сервер не зачеплено (перенесення йшло зі знімка M0). Архівом став чистий знімок M0; забруднений файл — окремо (§4). У коді: `conftest.py` задає тимчасову базу до будь-якого тестового модуля.

---

## 6. Журнал перевірок

- **Т4 (20.09):** нічне вікно 19.09 16:45 → 20.09 16:39 — 0 перезапусків, 0 watchdog, 0 дублікатів, цикл Active Queue ~143 с; сесії пережили ніч; AC4497EO відкритий обґрунтовано (ще в черзі). Відхилення від «чистої» ночі: ручні читання архіву 19.09 вдень і `REVOKE` (§4).
- **R1, жива перевірка (20.09, прийнято із зауваженням):** акаунти 1 і 2 — `success` з першої спроби (10:53:39, 10:55:51), акаунт 3 — з четвертої (10:56:06, 10:58:14, 10:58:25 → 10:59:58, ~4 хв). Вікно не відкривалось.
- **e-TOLL СТОП 2 (20.09):** дамп + відкладена копія; `GRANT CREATE ON DATABASE transfin_backend TO etoll_owner`, `CREATE EXTENSION btree_gist` (1.8), `GRANT CONNECT … TO etoll_recorder`; репетиція на відновленій копії (`upgrade → downgrade → upgrade`); код `9705ffc → 5a296e0`, міграція `e7011a5c3b19` застосована; пароль `etoll_recorder` згенеровано на сервері й записано в базу (SCRAM) і `transfin.env` без друку. Recorder запущено 18:12 UTC на `8530c53`. 10 хв: 112 опитувань, 0 невдалих, 17 пристроїв, 154 мс, 41 МБ пам'яті; 8 пристроїв із застиглим годинником у карантині (`too_old`, не множаться); у русі 0 тягачів. 17 прив'язок пристрій↔тягач **підтверджено власником 20.09** (`confirmed_by = 1`).
- **Мовчазні пристрої Lontex (20.09):** 8 із 17 не звітують 2–8 діб, 7 із них — на тягачах у черзі на кордон. Версія власника: водії знеструмлюють пристрій в Україні (RMPD/SENT потрібні лише в Польщі). Довідка для Lontex: `~/TransFin-private/lontex_silent_devices_20260920.md` (справжні IMEI — поза git).

---

- **P1–P8 + P5a, розгортання (21.09, 08:09–08:30 UTC):** дамп обох баз + відкладені копії `pre-p1p8-*_20260921T080917Z`; код `62bbf44 → 961387e` (12 комітів, без змін залежностей і коду recorder); репетиція `0006` на відновленій копії `transfin` (`upgrade → downgrade → upgrade`, хеш тіла тригера `5a7fe6… → a58f48… → 5a7fe6…`, перевірка: з актором — `rehearsal:p1p8`, без — `db_trigger`; копію видалено); `0006` на справжній `transfin` з `lock_timeout=5s`; `ALLOWED_HOSTS` і перезапуск сайту — **виконав власник** (автоперевірка Claude Code блокувала ці команди); перевірки: `/` 200, `/api/*` без входу 401, чужий Host 400, сесії пережили перезапуск; власник після перезавантаження сторінки відкрив усі розділи — 0×403, 0×5xx, 0 помилок застосунку; `/api/ledger/currencies` 200. Воркер: перший цикл Active Queue після перезапуску — `checked=3 saved=14 errors=0`. Recorder не чіпали: 0 перезапусків, `password=` — 0. Спостереження 14 хв (до 08:30 UTC): 432×200, 2×401 (перевірки без входу), 0×403, 0×5xx, 0 перезапусків сайту й recorder; два цикли Active Queue поспіль успішні, `last_success_at` усіх трьох акаунтів оновлюється.
- **Перенесення журналу (21.09, M0–M3):** M0 — Mac-сайт зупинено, знімок `123c23b5…` (не змінювався з 19.09); M1–M2 — репетиція на копії серверної бази: пробний прогін, `--apply`, повторний `--apply` (0 змін), незалежні SQL-перевірки (0 сиріт), `/api/journal` у коді застосунку — 28 рейсів і 16 відрізків; M3 — дамп + `pre-journal-*`, пробний прогін на продакшні (ті самі числа), `--apply` о ~09:13 UTC, ті самі перевірки; 2 PDF скопійовано й звірено за sha256. Власник перевірив: список, картки, CMR і заявка відрізка 21 відкриваються; «Відрізок 1 з 2» (того ж вечора власник повернув назву «рейс»: «Рейс 1 з 2»); логіст лишається після збереження; CMR — у браузері.
- **Ланцюжок «черга → перетин → журнал» (21.09, ~23:20–23:35 Київ):** сервер `ceb8991` (VIEWER + кнопка «Створити рейс»); воркер Mac перезапущено на новому коді (ліміт деталей, розклад); `GRANT UPDATE` на `crossing_log` — після цього 0 `permission denied`, деталі читаються по одній на хвилину в межах ліміту; розклад 10:30/16:00 створено (курсор ініціалізовано без зайвого читання); ручне читання за 14 днів — ВОТРАНС 7, Транс Левел 23 унікальних перетинів, **10 нових** (останній перетин 21.09 19:16 Київ); **15 перетинів готові до «Створити рейс»** (тягач зіставлено, рейсу немає). Колезі додано VIEWER.
- **Обліковий запис id 2 (перевірка 21.09, лише читання):** створений власником 17.09 12:11 UTC у Safari; з 12:12 по ~12:26 ним користувався **інший комп'ютер** (Mac, Chrome 152 — у власника того часу паралельно Chrome 153): 3 невдалі входи, вхід, перегляд усіх розділів, один запис — «запросити вхід» eCherha (таблицю акаунтів eCherha потім очищено й створено наново). Після деактивації — лише 401: вкладка опитувала сервер до 19.09 (інша мережа), останнє — невдалий вхід 19.09 21:14 Київ. Імовірно — сам працівник; nginx не записує користувача, тож це висновок, не доказ.

- **B — C1 для архіву (22.09, ~23:50 Київ 21.09):** `a0d53ee`; воркер перезапущено; ручне читання ВОТРАНС за 2 дні о 23:52 — 30 с, 2 сторінки, 2 наявні перетини, 0 помилок; у лозі під час читання вікно весь час згорнуте. Власник бачив спливання — за логом це старт Chrome трьох акаунтів після перезапуску (23:50–23:51, «три спалахи», §3) і старий код до перезапуску (23:47–23:48, деталі архіву в новій вкладці). 10 хв після читання — 0 спливань, 0 помилок. Деталі архіву наживо ще не перевірені (не було черги).
- **Автоматичні рейси з перетинів (22.09):** `6673439`, виправлення правила 2 `2f91a23` (власник: рейс 26 — внутрішній, ручний, без перетину). 15 перетинів без рейсу (14–21.09) → **15 нових чернеток, рейси 29–43**. Спершу перетин 34 правилом першої версії прив'язався до рейсу 26 — власник виправив правило; перетин 34 відв'язано й створено рейс 43 (JR №10) однією транзакцією з перевірками, рейс 26 не змінювався (остання зміна 13.09, перетину немає). Між версіями автоматику на Mac було вимкнено ~20 хв. Після: перетинів без рейсу (з тягачем) — 0.
- **e-TOLL етап 2 — розвідка (22.09, без входу):** `mojekonto.etoll.gov.pl` із сервера доступний; вхід — Keycloak `keyhub.mf.gov.pl` (realm `IKK`), сторінка входу малюється JS — потрібен браузер; внутрішній JSON-шлюз `akesbwrap.mf.gov.pl/rest/dk104/channel/selfservice` з кодами операцій (баланс, рахунки, тягачі, пристрої, історія проїздів, звіти). Playwright 1.48 у venv є, Chromium — ставимо (§ етап 2). Тимчасові файли розвідки на сервері видалено.
- **e-TOLL етап 2, розгортання (22.09, ~07:55–08:05 Київ):** дамп `pre-etoll2-transfin_backend_20260922T050232Z.dump` (78 таблиць даних); репетиція `c3e9a1f7d204` на відновленій копії (upgrade → downgrade → upgrade; власник усіх 8 таблиць — `etoll_owner`; веб-логін пише, recorder — без доступу; 17 пристроїв етапу 1 не зачеплено; копію й тимчасовий код видалено); код `2f91a23 → d30cb9d`; `uv pip install cryptography==43.0.3`; ключ у `transfin.env`; міграція з `lock_timeout=5s`; сайт перезапущено (health 200, нові маршрути без входу — 401, помилок у журналі 0); recorder не чіпали (`active`); служба `transfin-etoll-stage2` встановлена й запущена (паролів у журналі — 0). Перед цим — перевірка справжнього читача на кабінеті: ВОТРАНС 1/6/16, Транс Левел 18/19/75 (рахунки/тягачі/пристрої).

- **«Жодної тривоги без пояснення й дії» (22.09, ~09:55 Київ):** дамп `pre-alerts-transfin_backend_20260922T065146Z.dump` (86 таблиць); репетиція `d8f2b6a41c90` на відновленій копії (upgrade → downgrade → upgrade; копію видалено); код `ed4f61e → 691769c`; міграція (таблиця `alert_decisions`); сайт і `transfin-etoll-stage2` перезапущено, recorder не чіпали; health 200, помилок 0. Перевірка без входу: сторінка e-TOLL будує 18 тривог (невідомий в IKK — 2: AC2284BH, NU042183; в архіві, а в IKK активний — 2; пристрій не той — 1; не під контролем GPS — 4; мовчазні пристрої — 9), усі з чотирма частинами; на Перетинах тривог 0 (усі перетини з рейсами). У Черзі прибрано кнопку «E»; позначка балансу — праворуч рядка.

- **Wialon — розвідка доступу (23.09, з хмарної сесії Claude, НЕ з сервера transfin.uk):** підключення й читання пройшли напряму до `hst-api.wialon.com`, через партнера Wialon «Car-Service» (кабінет enter.car-service.net.ua) видано акаунт/токен; сам API-виклик — напряму на `hst-api.wialon.com`, без проксі. Токен існує й активний, використано виключно на читання (жодного запису), формальні права токена (чи обмежений до read-only) не перевірені. Отримано список 16 бортів TL (на 1 менше за 17 у Tekson); детально прочитано ~10: позиція/швидкість/одометр/пальне/історія (щонайменше кілька місяців) — узгоджується з незалежним джерелом (цифровий тахограф водія, звірка на AC0843EP/BK3752KA, розбіжність одометра 0,1–0,6%). Не вийшло: `report/get_report_templates` (обхід є), `core/batch_get_addresses` (обхідного шляху нема). Технічні деталі й пастки (калібрування пального через `tbl`, а не `p`; GPS-глітчі) — `backend/HANDOFF_ARCHIVE.md` (перенесено з HANDOFF §4 25.09).

- **Злиття перетинів і тягачі в рейсах (24.09, ~07:06–07:08 UTC):** робила інша сесія Claude Code (з теки проєкту в iCloud), root-ключем з Mac власника. Дамп `/var/backups/transfin/transfin_backend_before_crossing_merge_20260924_070633.dump` (20 МБ); за транскриптом тієї сесії: дублі перетинів злито (лишено записи еЧерги з перенесеною датою й правилом наказу), тягача проставлено 15 рейсам (46 з 46 з тягачем), автозавершення — зупинено на СТОП (закрило б 15 з 30 відкритих). Коміту з кодом немає. У базі 25.09 перевірено лише `crossing_log` = 44. **Дамп не видаляти, поки не розберемося** (власник, 25.09).

- **Автозавершення рейсу — крок «а»: злиття й розгортання з вимкненим перемикачем (25.09, 07:27 Київ).** `feat/trip-auto-close` → `ui/journal-redesign` (fast-forward `6e416a7 → 4cb732e`, push прийнято); цільові тести — 117 пройдено, 0 падінь. Копія нічного дампу на Mac: `~/TransFin-private/backups/transfin_backend_20260925T032244Z.dump` (21 395 265 байт, `600`, sha256 `bcb16da4…de36f8`, збігається із серверним). Сервер `474abcd → 4cb732e`, міграцій немає, перезапущено лише `transfin` (04:27:20 UTC); `/` 200, `/health` 200, `/api/journal/alerts` без входу 401, помилок у журналі 0. Знімок до й після (і після першого циклу воркера) однаковий: рейси 46 (12 active / 12 closed / 22 draft), плечей 38, `alert_decisions` 4, тягачів 27, причепів 35, `crossing_log` 44 (9 без `truck_id`), alembic `a1e6d8c4b937`. Воркер: у `worker.env` `QUEUE_JOURNAL_AUTO_CLOSE` немає (вимкнено); контрольований перезапуск о 07:35 Київ (`kickstart -k`, код `4cb732e` з `~/Projects/backend` на `ui/journal-redesign`), перший цикл Active Queue — `checked=3 saved=7 errors=0`, 61 с. Логістам про нові тривоги Журналу не повідомляли — власник спершу переглядає сам.
- **Воркер, код виходу 8 (перевірка 25.09, лише читання).** Код 8 = «не вдалося дістатися бази, щоб узяти lock» (`EXIT_DB_UNAVAILABLE`, `app/queue_worker.py`). За добу до 07:34 25.09 — **39 виходів із кодом 8** (усі 24.09, 09:05–21:21) і ~20 справжніх перезапусків; з 21:22 24.09 до контрольного перезапуску — жодного. Причина: сон Mac (за `pmset` за добу 75 засинань, 17 пробуджень) — тунель рветься, після пробудження воркер стартує раніше, ніж тунель піднявся, і launchd перезапускає його кожні 30 с, поки тунель не відновиться. Вранці 25.09 Mac на мережі, батарея 20% і заряджається. Окремо: **тести пишуть у живий лог воркера** `queue_worker.app.log` — «спалахи» по 4–5 стартів за секунду (24.09 07:50, 25.09 06:30–06:58) з `port 1` і CRITICAL «lock lost» — це прогони тестів, а не воркер. Не виправлялося.

- **Автозавершення — крок «в»: заднім числом 8 рейсів + `truck_id` у 9 перетинах + `trailer_id` у 3 рейсах (25.09, ~07:50 Київ; дозвіл власника).** Дамп перед зміною: `/var/backups/transfin/transfin_backend_before_auto_close_apply_20260925_044939.dump` (21 476 940 байт, `root 600`, 110 таблиць даних, `pg_restore --list` — ок), копія на Mac `~/TransFin-private/backups/` — sha256 `5835537b…d95fc5fd` збігається. Тека бекапів — `root:root 700` (нічний скрипт щоночі ставить ці права, §1) — `pg_dump -f` від `postgres` не може створити файл; дамп знято через stdout від root (права теки не змінював). **Результат (04:50–04:53 UTC, код `4cb732e`):** `journal_auto_close --apply` — закрито 8 (id 4, 7, 9, 12, 14, 16, 24, 25; номер компанії закріплено), 7 порожніх чернеток лишились `draft` (тривога «Видалити / Лишити»); `crossing_truck_backfill --apply` — 9 з 9 (`matched`, +9 у `match_log`); `trailer_id` однією транзакцією з перевірками до/після: рейс 1 ← 11 (AC8395XF, перетин 18), 3 ← 10 (AC1381XG, перетин 31), 10 ← 33 (AC3978XG, перетин 27; у самому перетині `trailer_id` порожній — знайдено за номером), по запису `system:trailer_backfill` в історії. Знімок до → після: рейси active 12 → 4, closed 12 → 20, draft 22 → 22; історія 4 → 15 (8 `system:auto_close` + 3 `system:trailer_backfill`); перетини без тягача 9 → 0. **Не змінилось:** плечі 38, оплати (paid 12 / unpaid 26), документи (received 2 / not_received 36), суми (EUR 28 700, UAH 587 797,08, USD 50 050), md5 грошей і статусів плечей, `alert_decisions` (4, той самий md5). Сайт `active`, помилок 0. Перемикач `QUEUE_JOURNAL_AUTO_CLOSE` (крок «г») **не вмикали**. **Причина в історії цих 8 рейсів — час перетину в UTC** («Завершено автоматично: новий перетин 15.09.2026 23:56» = 16.09 02:56 Київ); записи не змінюємо (власник, 25.09). Новий код (гілка `journal-alert-title`, `271e8f7`, не розгорнуто) пише час за Києвом з позначкою «(Київ)».

- **Автозавершення — крок «г»: перемикач увімкнено (25.09, 08:05 Київ; дозвіл власника).** Гілку `journal-alert-title` злито в `ui/journal-redesign` (`013db18` — спершу `ui/journal-redesign` влито в гілку фічі merge-комітом, бо вона пішла вперед на коміт HANDOFF; далі fast-forward, історія не переписувалась). Цільові тести — 156 пройдено, 0 падінь. Сервер `4cb732e → 013db18`, міграцій немає, перезапущено лише `transfin` (05:05 UTC): `/` 200, `/health` 200, `/api/journal/alerts` без входу 401, помилок 0. Знімок до розгортання = знімок після 3в (крім `match_log` +8 — звичайна робота воркера). `worker.env`: копія `worker.env.bak-autoclose-202609250805`, додано рядок `QUEUE_JOURNAL_AUTO_CLOSE=true` (решта файлу без змін — `cmp`); контрольований перезапуск воркера 08:05:30 (pid 99395 бачить `QUEUE_JOURNAL_AUTO_CLOSE=true`); перший цикл Active Queue — `checked=3 saved=7 errors=0`. Перевірка новим кодом на продакшені (лише читання): закрилось би **0**, тривог Журналу **7** — порожні чернетки id 1, 6, 10, 11, 13, 22, 30, заголовки з номером компанії й датою за Києвом (напр. «AC1675HX знову виїхав 14.09 — чернетка ВТ 3/2026 порожня»). **Коли працює:** автозавершення воркер запускає після кожного читання архіву eЧерги (розклад 10:30 / 16:00 + наздоганяння після старту), не в циклі черги; при 0 закриттів у лог нічого не пише, кожне закриття — `Journal auto close trip_id=… crossing_id=…`. Вимкнути — прибрати рядок з `worker.env` і `kickstart -k`.

- **Тривога «низький баланс»: обидва кабінети, рахунок за компанією й номером (25.09, 09:46 Київ; дозвіл власника).** Код `c62f24b` (гілка `etoll-low-balance-both-cabinets`, fast-forward у `ui/journal-redesign`; 3 нові тести на PostgreSQL падають на старому запиті; тести e-TOLL — 105 пройдено). Сервер `013db18 → c62f24b`, міграцій немає, перезапущено лише `transfin` (06:46 UTC): `/` 200, `/health` 200, помилок 0. Воркер не чіпали — тривогу рахує сайт (`ikk_overview`). Результат `low_accounts` на продакшені (лише читання): **до — 4** (усі Транс Левел: AC0953HA 73,45; AC4497EO 70,49; AC4586BX 132,95; AC4845HP 13,08 PLN), **після — 9**: ті самі 4 + 5 тягачів ВОТРАНС на рахунку «Prepay» 106294 (158,36 PLN; AC1675HX, AC2218CE, AC4293BA, AC6545HH, AC8587HT) — раніше кабінет ВОТРАНС випадав. На один рахунок — окрема тривога на кожен тягач.

- **«Перевірка e-TOLL» — міграція `d8e3b5a1c946` (25.09, ~11:45 Київ; дозвіл власника).** Коміт `34276fa` (лише міграція й її тести), `ui/journal-redesign` fast-forward `c62f24b → 34276fa`, пуш прийнято; тести e-TOLL — 448 пройдено, 1 пропущено (PostGIS локально немає). Дамп `/var/backups/transfin/transfin_backend_before_d8e3b5a1c946_20260925_074311.dump` (21 674 902 байти, 110 таблиць даних, `pg_restore --list` — ок, sha256 `d45b3acb…efd88ad8`), копія на Mac `~/TransFin-private/backups/` — sha256 збігся (переніс власник; `scp` завис на 47 хв, потік `ssh … cat` — секунди, рецепт у §9 оновлено). Репетиція на відновленій копії (тимчасова база `transfin_rehearsal_d8e3b5a1c946`, код з `git archive 34276fa`, живу теку не чіпали): `upgrade → downgrade → upgrade` — ок; `report_runs` 18 / `report_rows` 455 і md5 журналу запитів однакові на всіх кроках; власник нових таблиць `etoll_owner`, сайт пише, recorder — ні; тимчасове прибрано. **Продакшн:** код `c62f24b → 34276fa`, `alembic upgrade` (lock_timeout 5s, statement_timeout 60s) `a1e6d8c4b937 → d8e3b5a1c946`, `PostgresqlImpl`; перезапущено лише `transfin`. Після: `/` 200, `/health` 200, `/api/journal/alerts` без входу 401, https 200, помилок 0; stage2 і recorder `active` (не перезапускались); `report_runs` 18 (усі `purpose = manual`, md5 до = після), `report_rows` 455; `plate` у `report_runs` — nullable, індекс `report_runs_account_period` є. Нові таблиці `toll_checks`, `toll_check_sections` — порожні; коду, що в них пише, ще немає. Воркер не чіпали (не потрібен). `password=` у журналі recorder — 7, усі 20.09 17:44–17:45 (інцидент §5), нових — 0; рядок перевірки в §1 уточнено.

- **«Перевірка e-TOLL» — міграція `f1c7a3d9b520`: ланцюжок звіту + облікові записи Wialon (25.09, ~13:45 Київ; дозвіл власника).** Коміт `0135958` (лише міграція й тести), `ui/journal-redesign` fast-forward `34276fa → 0135958`, пуш прийнято; тести e-TOLL — 457 пройдено, 1 пропущено (PostGIS локально немає). Дамп `/var/backups/transfin/transfin_backend_before_f1c7a3d9b520_20260925_103257.dump` (21 866 626 байт, 112 таблиць даних, `pg_restore --list` — ок, sha256 `992704c5…68325f8`), копія на Mac потоком `ssh … cat`, у `~/TransFin-private/backups/` переніс власник — sha256 збігся (разом перевірено й копію `d8e3b5a1c946`, `d45b3acb…efd88ad8`). Репетиція на відновленій копії (код `0135958` з `git archive`, тимчасова база, живу теку не чіпали): `upgrade` до `f1c7a3d9b520` → `downgrade` до `a1e6d8c4b937` (обидві міграції) → `upgrade` до `f1c7a3d9b520` — ок; `report_runs` 19 / `report_rows` 473 і md5 журналу запитів однакові на всіх кроках; тимчасове прибрано. **Продакшн:** код `34276fa → 0135958`, `alembic upgrade` (lock_timeout 5s, statement_timeout 60s) `d8e3b5a1c946 → f1c7a3d9b520`, `PostgresqlImpl`; перезапущено лише `transfin`. Після: `/` 200, `/health` 200, `/api/journal/alerts` без входу 401, https 200, помилок 0; stage2 і recorder `active` (не перезапускались); `report_runs` 19 (md5 до = після), `report_rows` 473; нові/перебудовані таблиці `gps_accounts`, `toll_check_sections` (власник `etoll_owner`, три унікальні індекси від дублікатів доказу), `toll_checks` — порожні. Права на токен Wialon: сайт (`transfin_backend`) SELECT на `token_enc` — **ні**; `etoll_recorder` — читає токен, UPDATE лише колонок стану (на `token_enc` — ні). `password=` у журналі recorder після 20.09 17:46 — 0. Токенів ще немає: вводить власник у картці компанії (крок 3); ключ — окремий `ETOLL_GPS_SECRET_KEY`, окремий файл налаштувань для служби запису — план перед зміною (власник, 25.09).

- **«Перевірка e-TOLL» — код розгорнуто й увімкнено (25.09, ~14:45–14:50 Київ; дозвіл власника).** Коміти `9586f14` (код і тести: трек GPS + ланцюжок звіту, розклад 07:00/12:00 у службі `transfin-etoll-stage2`, вкладка «Проїзди за вчора» лише SUPER_ADMIN) і `941a329` (AGENTS §13); `ui/journal-redesign` fast-forward `0135958 → 941a329`, пуш прийнято; тести e-TOLL + політика доступу + сторінки — 570 пройдено, 1 пропущено; повний прогін — 15 помилок, ті самі, що в `tests/KNOWN_FAILURES.txt`. Міграцій немає (alembic `f1c7a3d9b520`). Перед розгортанням — сухий прогін на продакшені лише читанням (тимчасова тека, `SET TRANSACTION READ ONLY`, кабінет не питали) за 24.09: 9 тягачів у Польщі, AC8587HT ⚠️ «Łomna → Sadowa», AC4293BA ✅; знайдено й виправлено стрибок GPS, через який один тягач рахувався 19 хв. **Розгортання:** код `0135958 → 941a329` (робоча тека чиста), перезапущено `transfin` і `transfin-etoll-stage2` (11:46 UTC, між читаннями кабінетів 11:40 і ~13:08): `/` 200, `/health` 200, `/api/journal/alerts` і `/api/etoll/checks` без входу 401, https 200, помилок 0; recorder не чіпали. **Увімкнення:** копія `/etc/transfin/transfin.env.bak-etollcheck-202609251146` (`root:transfin 640`), дописано `ETOLL_CHECK_ENABLED=true`, `ETOLL_CHECK_AUDIENCE=owner`, `ETOLL_CHECK_FIRST_DAY=2026-09-25` (решта файлу без змін — `cmp`), перезапущено `transfin-etoll-stage2` і `transfin`; служба бачить увімкнено / owner / 25.09; сьогодні нічого не запускає (`report_runs` з `purpose check_*` — 0, `toll_checks` — 0), перша ранкова перевірка — 26.09 о 07:00 Київ за добу 25.09, повторна — 12:00. **Вимкнути** — прибрати три рядки (або повернути копію) і `systemctl restart transfin-etoll-stage2 transfin`. Тягачі без GPS — у перевірці за журналом перетинів (виїзд через польський перехід за 14 днів), до 6 додаткових звернень до кабінету щоранку (власник, 25.09: лишаємо).

- **Перевірка реєстрацій черги — міграція `b2d9e4f7a613` (25.09, ~17:00 Київ; дозвіл власника).** Привід: AC1934EO / AC3915XG, Ягодин, порожній («Ви вказали, що їдете без вантажу»), а стоїть у смузі «для вантажівок ≥ 7,5 тонн» — на Ягодині є окрема смуга для порожніх. Коміт `eb5bd94` (лише міграція, модель, тести): одна нова таблиця `queue_registration_checks` — стан вантажу й кількість МД-2 з реєстрації, смуга на момент читання; даних водія немає. Повний прогін після коміту — 4060 пройдено, 15 помилок, ті самі, що в `tests/KNOWN_FAILURES.txt`. Дамп `/var/backups/transfin/transfin_backend_before_b2d9e4f7a613_20260925_133025.dump` (22 007 204 байти, 113 таблиць даних, sha256 `7d3de600…f99d8e1`) і свіжий перед самою зміною `transfin_backend_before_b2d9e4f7a613_20260925_135530.dump` (22 031 432 байти, 113 таблиць даних, `pg_restore --list` — ок, sha256 `d70f5059…fbef06ee`); обидва — потоком `ssh … cat` у scratchpad сесії, `mv -n` у `~/TransFin-private/backups/` переніс **сам Claude Code** (перший раз — нове правило AGENTS §15 і `.claude/settings.local.json`, власник 25.09), суми збіглись. Репетиція на відновленій копії першого дампу (тимчасова база, код з `git archive eb5bd94`, живу теку не чіпали): `upgrade → downgrade → upgrade` — ок; таблиці 115 → 116 → 115 → 116, `queue_snapshots` 5660, `crossing_log` 46, `trip_journal` 48 і md5 `alert_decisions` однакові на всіх кроках; власник таблиці `transfin_backend`; `transfin_worker` — SELECT/INSERT/UPDATE/DELETE і послідовність (default privileges), `etoll_recorder` — нічого; тимчасове прибрано. **Продакшн:** push `1562626 → eb5bd94` в `ui/journal-redesign` (fast-forward; погодив власник у вікні — режим Manual), сервер `941a329 → eb5bd94` (робоча тека чиста), `alembic upgrade` (lock_timeout 5s, statement_timeout 60s) `f1c7a3d9b520 → b2d9e4f7a613`, `PostgresqlImpl`; до = після: `crossing_log` 46, `trip_journal` 48, `trip_journal_legs` 40, md5 `alert_decisions` `dd550488…`; таблиця порожня, права — як на репетиції. Перезапущено лише `transfin` (14:02 UTC): `/` 200, `/health` 200, `/api/journal/alerts` і `/api/queue/active` без входу 401, https 200, помилок 0; stage2 і recorder `active` (не перезапускались). Воркер не чіпали. **Код перевірки (читання реєстрації, правило, червона картка в Черзі) ще не розгорнуто** — чекає окремого «так» власника; воркер її не запускає, доки в `worker.env` немає `QUEUE_REGISTRATION_CHECK_ENABLED=true`.
- **Перевірка реєстрацій черги — код розгорнуто й увімкнено (25.09, 17:05–17:07 Київ; дозвіл власника).** Коміти `140b927` (код і тести) і `f0ccaa5` (AGENTS §15); повний прогін — 4098 пройдено, 15 помилок, ті самі відомі. `ui/journal-redesign` fast-forward `eb5bd94 → f0ccaa5` (погодив власник у вікні). Сервер `eb5bd94 → f0ccaa5`, міграцій немає, перезапущено лише `transfin`: `/` 200, `/health` 200, `/api/queue/active` і `/api/queue/registration-checks/decide` без входу 401, https 200, помилок 0. **Воркер** (OPS §3): `~/Projects/backend-worker` `cca865f → f0ccaa5` (`pull --ff-only`, тека була чиста); копія `~/.config/transfin/worker.env.bak-regcheck-202609251706`, дописано рядок `QUEUE_REGISTRATION_CHECK_ENABLED=true` (перші 551 байт = копія); `kickstart -k` 17:06:59, pid 36779. Перші цикли: читання реєстрації ~21–22 с (з них 10 с — `list_idle_timeout`), 3 за цикл (бюджет 60 с), `Active Queue cycle took` 128,4 / 126,2 / 124,1 с (межа 300 с); 12 реєстрацій, до 17:29 прочитано 9. AC1934EO — 🔴 «без вантажу у смузі для вантажівок», власник поставив «Навмисно»; AC2418HT — з вантажем, 2 МД-2, звичайна. **Вимкнути** — прибрати рядок з `worker.env` (або повернути копію) і `kickstart -k`.
- **Відображення (25.09, ~17:45 Київ; дозвіл власника):** `a355393` (червоний рядок «НЕВІРНО ЗАРЕЄСТРОВАНО» лишається й після «Навмисно»; на телефоні — на всю ширину картки; текст червоної картки — фіксований світлий колір) і `41bfa6f` (HANDOFF); повний прогін — 4099 пройдено, 15 помилок, ті самі відомі. `f0ccaa5 → 41bfa6f`, перезапущено лише `transfin`: `/` 200, `/health` 200, https 200, помилок 0. Воркер не оновлювали — його поведінка не змінилась.
- **Компактні картки Черги (25.09, ~18:15 Київ; дозвіл власника):** `c36298f` — одне слово в першому рядку («Порожній ✓» / «З вантажем ✓ · МД-2: 2» / «Не перевірено»), при розбіжності лише червоний рядок із «Навмисно дд.мм гг:хх» у кінці, без порожньої смуги над ним, дата · час · пункт — завжди один рядок (телефон — короткі форми). Повний прогін — 4104 пройдено, 15 помилок, ті самі відомі. `41bfa6f → c36298f` (погодив власник у вікні), перезапущено лише `transfin`: `/` 200, `/health` 200, `/api/queue/active` без входу 401, https 200, помилок 0. Воркер не чіпали.
- **Сіра палітра Черги й статус реєстрації лише тоді, коли він важливий (26.09, 07:41 Київ; дозвіл власника):** `c1ce441` — сірий список з одним акцентом, червоним; попередження одним рядком (червона крапка · червоний текст · сіре пояснення), «Час заїжджати до ММПП · до скасування …» одним рядком, у даті «Зараз · до ММПП»; невірна реєстрація — уся картка червона до «Навмисно», після — сіра з червоним рядком «НЕВІРНО ЗАРЕЄСТРОВАНО: …»; при збігу — жодного статусу; «реєстрацію не перевірено» — лише коли до заїзду ≤ `QUEUE_REGISTRATION_UNCHECKED_VISIBLE_HOURS` (типово 12); e-TOLL без плашки, низький баланс — червона сума з крапкою (`ETOLL_QUEUE_LOW_BALANCE_PLN`, типово 100; у `transfin.env` нічого не дописано); D/R/T 26×26; найсвітліший сірий `#8a8e96` (4,5:1). Цільові тести — 881 пройдено; повний прогін (до останньої правки кольору) — 4104 пройдено, 15 помилок, ті самі відомі. Push `a36a627 → c1ce441` (погодив власник), сервер `c36298f → c1ce441` (тека чиста, міграцій немає), перезапущено лише `transfin` (04:41 UTC): `/` 200, `/health` 200, `/api/queue/active`, `/api/etoll/truck-balances`, `/api/queue/workload` без входу 401, https 200, помилок 0. Воркер не чіпали.
- **⚠️ `transfin-etoll-stage2` у циклі OOM (виявлено 26.09 ~07:42 Київ, лише читання; НЕ пов'язано з розгортанням `c1ce441`):** з 04:00:59 UTC (07:00 Київ — перша ранкова «Перевірка e-TOLL») процес python служби впирається в `MemoryMax=768M` і його вбиває ядро; systemd перезапускає (`Restart=on-failure`), до 04:41 — 35 разів. Перед смертю в журнал нічого не пише. Кабінет IKK у циклі не смикається (останнє `ikk_runs` read — 03:42 UTC, до циклу); `report_runs` за 6 год — 0, `toll_checks` — 0. Сайт, recorder, nginx, PostgreSQL — 0 перезапусків, вільно ~2,5 ГБ. Наслідки: ранкова перевірка не відбулась, читання балансів IKK (раз на 2 год) стоїть. Рішення — за власником.
- **«Перевірка e-TOLL» вимкнено (26.09, 07:59 Київ; дозвіл власника):** копія `/etc/transfin/transfin.env.bak-etollcheckoff-202609260459` (`root:transfin 640`), `ETOLL_CHECK_ENABLED=true → false` (решта 19 рядків без змін — `diff`), перезапущено лише `transfin-etoll-stage2` (04:59:46 UTC): `active`, 0 перезапусків, процес бачить `false`. Читання IKK відновилось: `ikk_runs` 99 (ВОТРАНС) о 05:10:47 UTC — `ok`, баланс записано. **Пік пам'яті служби під час самого читання кабінету — 734 МБ з `MemoryMax=768M`** (Chromium у тому самому cgroup): запас 34 МБ і без перевірки. Пошта: `mail_accounts` = 0 — модуль не налаштований (§7), відновлювати нічого. `AUDIENCE`/`FIRST_DAY` не чіпали.
- **Діагноз OOM (26.09, лише читання):** перша справжня перевірка — 26.09 07:00 за 25.09 (`FIRST_DAY=2026-09-25`, до 07:00 списки днів порожні). Процес гинув ще до першого тягача: `report_runs` з 04:00 — 0, `toll_checks` — 0. Причина: уся карта платних ділянок у пам'яті Python — 2 073 ділянки, **1 608 424 вершини** (jsonb 23 МБ); локальний замір тим самим кодом на вивантаженій карті: розбір рядків ~520 МБ → ділянки ~634 МБ → `index_for` **~965 МБ піку** > 768 МБ. `_map()` ще й перечитує карту для кожного тягача; `candidates()` рахує весь парк до першого запису, без жодного рядка в журналі; після кожного перезапуску все спочатку. Сухий прогін 25.09 (за 24.09) робився окремо, поза cgroup служби (без межі 768 МБ) — тому не показав. Втрачено: ранкова перевірка за 25.09; читання IKK 04:00–04:59 UTC не випало (за розкладом було о 05:10).
- **«Завантаженість» — каталог сам заповнюється (26.09, 08:01 Київ; дозвіл власника):** `7a32ff6` (гілка `queue-workload-catalog-discovery`, fast-forward у `ui/journal-redesign`, пуш прийнято; повний прогін — 4113 пройдено, 15 помилок, ті самі відомі). Сервер `4d580a4 → 7a32ff6`, міграцій немає, перезапущено лише `transfin` (05:01 UTC): `/` 200, `/health` 200, `/api/queue/workload` і `/api/queue/workload/status` без входу 401, https 200, помилок 0. **Воркер** (§3): `~/Projects/backend-worker` `f0ccaa5 → 7a32ff6` (тека чиста), `kickstart -k` 08:01:28; `Queue worker starting` 08:01:30, перший цикл `checked=3 saved=11 errors=0` (129 с), о 08:03:41 — «catalog is empty — reading the workload board» → **каталог 38 пунктів**, під наглядом 0 (вибирає власник). `QUEUE_WORKLOAD_CATALOG_DISCOVERY_HOURS` у `worker.env` не задано — типово 6.
- **Короткі назви смуг і статус кружечком (26.09, 08:24 Київ; дозвіл власника):** `8179905` (гілка `queue-lane-names-status-dot`, fast-forward у `ui/journal-redesign`; повний прогін — 4134 пройдено, 15 помилок, ті самі відомі). У рядку дати — «пункт UA · коротко» за правилами `QUEUE_LANE_SHORT_NAMES` (типові правила власника в коді налаштувань; у `transfin.env` нічого не дописано), невідома смуга — повністю; кружечок статусу: зелений / червоний (сесія потребує уваги або дані старші за `QUEUE_STATUS_STALE_MINUTES`, типово 20), текст — у підказці й угорі панелі «Джерела eЧерги». Сервер `7a32ff6 → 8179905`, міграцій немає, перезапущено лише `transfin` (05:24 UTC): `/` 200, `/health` 200, `/api/queue/active` і `/api/queue/health` без входу 401, https 200, помилок 0. Воркер не чіпали.

## 7. Чекає на власника

- AC2284BH: наш старий тягач, свідомо в IKK ВОТРАНС для мобільного застосунку e-TOLL — на сторінці e-TOLL натиснути «Навмисно в IKK» з поясненням. NU042183 (Транс Левел) — теж невідомий в IKK: вирішити.
- Листи e-TOLL: окрема скринька з пересиланням лише листів `powiadomienia@etoll.gov.pl`; до неї модуль листів не вмикається (доступу до `trans1321@gmail.com` сервер не отримує).
- 15 старих порожніх чернеток журналу (§2) — вирішити, що з ними робити.
- AC5801EP: активний в IKK без пристрою, у нас в архіві (§2) — деактивувати в IKK чи повернути в роботу.
- Очистити Кошик Mac від старих файлів e-TOLL (`~/.Trash/etoll-old-20260922`).
- Надіслати довідку Lontex (§6) і з'ясувати: напруга живлення, час останнього пакета e-TOLL і стани SENT через API.
- Після 27.09: видалити копії `transfin.env` і забруднену тестами базу Mac (§4).
- Перевірити зарядку Mac (§3): 21.09 ~23:20 Київ Mac знову був на батареї, хоча власник вважав, що на зарядці.
- Решта даних лише на Mac (документи й дозвільна база, `order_log`) — аналіз, що переносити (план, §9, потік 2; мета — повністю відмовитися від Mac-сайту).
- **Wialon:** перевірити доступ до `hst-api.wialon.com` САМЕ з сервера transfin.uk (у червні була відмова 403 з іншої адреси — можливе обмеження за IP на боці акаунту Wialon); чи можна обмежити токен до read-only. Токен — **не в `transfin.env`**, а в картці компанії (розділ «Підключення Wialon», окремо ВОТРАНС і Транс Левел, зашифрований, назад не віддається; рішення власника 24.09). Car-Service — партнер Wialon, кабінет enter.car-service.net.ua (з'ясовано 23.09).
- **Резервні копії поза сервером** (план — HANDOFF §5, погоджено 25.09): створити акаунти Hetzner Storage Box (HEL) і Backblaze B2 (EU), пароль сховища на папері.
- **Злиття перетинів 24.09:** розібратися з результатом (§6); до того дамп `transfin_backend_before_crossing_merge_20260924_070633.dump` не видаляти.

## 8. Інфраструктура й гігієна

- **fail2ban: `inactive`** (перевірено 25.09) — відкрите завдання. Вхід по SSH за паролем вимкнено (§1), ufw `active`.
- **Бекапи поза сервером** — немає; restic, borg, rclone на сервері не встановлено (перевірено 25.09). Time Machine на Mac — немає; `worker.env`, pgpass і ключ тунелю на Mac ніде не бекапляться.
- **Вкладення відрізків** (`storage/received_attachments` на сервері) у нічний `pg_dump` не входять — додати в бекап (технічний борг, HANDOFF §6).
- **nginx не пише час відповіді** (`$request_time`) — під час зависання 21.09 тривалість запитів не було видно (технічний борг, HANDOFF §6).
- Стара копія репозиторію `~/Desktop/backend.MOVED-20260918` (в iCloud, у `backups/` — персональні дані): видалити лише з дозволу власника.
- **~95 копій баз із персональними даними в iCloud** (`~/Desktop`) і відкриті секрети на Mac: `~/.transfin_config.json`, паролі в `*.disabled`, `.env` в iCloud, `TELEGRAM_BOT_TOKEN` у `~/.zshrc`. Рішення власника; нікуди не копіювати й не друкувати. Права на ці файли — перевірити (як для ключів ПриватБанку).
- Скрипти на Mac поза git: `~/transfin_tekson_setup.sh` (запис доступу Tekson: значення лише через stdin, атомарна заміна файла, решта рядків звіряється `cmp`, копія лишається); `~/transfin_chrome_cleanup.sh` (прибирання клонів Chrome: без аргументів — показ, `--apply` — видалення лише `code_sign_clone.*` старших за 60 хв і не відкритих, `--install-agent` — щоденно о 05:30 через LaunchAgent `com.transfin.chrome-clone-cleanup`, лог `~/Library/Logs/TransFin/chrome_clone_cleanup.log`).
- Старі десктопні інструменти (`transfin.py`, `zvirka.py` і їхні копії) перейменовано в `.disabled` 18–19.09, щоб не читали eCherha паралельно з воркером.

### Захист гілки `ui/journal-redesign` (23.09)

GitHub Pro, репозиторій `trans1321-wq/transfin-backend`: зміни лише через pull request зі схваленням власника, **force push заборонено**. У Bypass list — **Repository admin (Always allow)**.

- **Власник і Claude Code від його імені** комітять і пушать прямо в гілку, як раніше. Перевірено 23.09 пробним комітом `c092488`: push прийнято. GitHub при цьому друкує рядок `remote: - Changes must be made through a pull request.` — це повідомлення про правило, а не відмова; push проходить завдяки Bypass list. Відмова виглядала б інакше: `! [remote rejected]` і гілка на сервері лишилася б на старому коміті (перевіряти `git ls-remote origin ui/journal-redesign`).
- **Віталій (swift1421@gmail.com)** — фінансовий облік, власний Claude Code — працює **через pull request**: гілка від `ui/journal-redesign` → PR → схвалення власника → злиття.
- Якщо push колись справді відхилять — показати власникові текст помилки, **не обходити** (ні force, ні зміною правил).

## 9. План (власник, 22.09) — з операційними деталями

Коротка версія без деталей — HANDOFF §5. Порядок: Потік 1 по черзі; зауваження власника до Журналу — пакетом між етапами поточної роботи (блокуючі — одразу). Кожна міграція: прогін на відновленій копії продакшену (upgrade → downgrade → upgrade, скрипти в scratchpad сесії), `pg_dump` у `/var/backups/transfin/` (root 600), міграція **до** перезапуску `transfin.service` (застосунок робить `create_all` на старті), `PGOPTIONS="-c lock_timeout=5s"`.

#### Рецепт розгортання з міграцією (22.09, після двох помилок цього дня)

**Правило: alembic на сервері запускають ЛИШЕ з налаштуваннями сервісу (`EnvironmentFile=/etc/transfin/transfin.env`), ніколи без них.** 22.09 міграцію запустили без нього — alembic мовчки взяв локальну SQLite-базу за замовчуванням і почав мігрувати НЕ ТУ базу (продакшн-Postgres уцілів випадково, бо ланцюжок упав на другому кроці). Тепер `alembic/env.py` без `DATABASE_URL` **відмовляється працювати** (`app/alembic_guard.py`, тест `test_alembic_requires_database_url.py`); локальний дефолт самого застосунку лишився.

```bash
# 1. Резервна копія. Тека — root:root 700 (§1), тож pg_dump від postgres
#    пише в stdout, а файл створює root (umask 077 → 600), як нічний скрипт.
ssh root@46.224.18.229 'set -eo pipefail; umask 077; TS=$(date -u +%Y%m%d_%H%M%S); \
  F=/var/backups/transfin/transfin_backend_before_<ревізія>_$TS.dump; \
  sudo -u postgres pg_dump -Fc -d transfin_backend > $F; \
  pg_restore --list $F >/dev/null && ls -l $F && sha256sum $F'

# 1б. Копія на Mac (AGENTS §15) — ПОТОКОМ через ssh, не scp: 25.09 scp з
#     сервера на Mac завис на 47 хв без жодного байта, `ssh … cat` того ж
#     файлу (21,7 МБ) — за секунди. Потім звірити sha256 з кроком 1.
F=transfin_backend_before_<ревізія>_<час>.dump
( umask 077; ssh root@46.224.18.229 "cat /var/backups/transfin/$F" > ~/TransFin-private/backups/$F )
shasum -a 256 ~/TransFin-private/backups/$F

# 2. Код
ssh root@46.224.18.229 'cd /home/transfin/backend && sudo -u transfin git fetch -q origin \
  && sudo -u transfin git merge -q --ff-only origin/ui/journal-redesign \
  && sudo -u transfin git log --oneline -1'

# 3. Міграція — ДО перезапуску (застосунок робить create_all на старті),
#    з налаштуваннями сервісу і віртуальним оточенням .venv (НЕ venv).
ssh root@46.224.18.229 'cd /home/transfin/backend && set -a && . /etc/transfin/transfin.env && set +a \
  && sudo -u transfin env DATABASE_URL="$DATABASE_URL" PGOPTIONS="-c lock_timeout=5s" \
     .venv/bin/alembic upgrade head'

# 4. Перезапуск і перевірка
ssh root@46.224.18.229 'systemctl restart transfin && sleep 5 && systemctl is-active transfin \
  && curl -s -o /dev/null -w "%{http_code}\n" -H "Host: transfin.uk" http://127.0.0.1:8000/ \
  && sudo -u postgres psql -d transfin_backend -X -t -c "select version_num from alembic_version;" \
  && journalctl -u transfin -p err --since "3 min ago" --no-pager | tail -5'
```

Перевірка після кроку 3: `select version_num from alembic_version` має показати нову ревізію; якщо в лозі видно `Context impl SQLiteImpl` — команда пішла не в ту базу, зупинитись.

**Стан продакшену на кінець 23.09:** код `27ed4f0`, alembic `a1e6d8c4b937`. Розгортання 23.09 (кожна міграція — з резервною копією `transfin_backend_before_<ревізія>_<час>.dump`): `bb281e9` (alembic лише з налаштуваннями сервісу), `21b0c72` + `f3b8c1d5e207` (звіт e-TOLL, етап 1), `f2ece34` + `a9c4e2b7f318` (карта платних ділянок), `c6b41ee` (панелі e-TOLL у самій сторінці, помилка малювання видима), `1943fac` (сторінка e-TOLL отримала ключ прав на зміну), `36e2bae` (версія інтерфейсу: no-store, `?v=` у скриптах, самоперевірка сторінки), `2c0d796` (гортання місяців у календарі IKK), `19275e9` (обліковий запис обирається першим), `1d2d094` + `d4a7c2e8b615` (гроші рядка звіту), `7789d8d` + `e5c8b3f1a742` (вивантаження CSV, чорний список, час у двох виглядах), `8a0cf10`, `82b7964` (дозаповнення грошей, деталі одного рядка — свої), `7da7327` + `f7d2c5a9b834` (тривога на несплачене, тотожність проїзду за моментом UTC), `fcc1d5f` + `a1e6d8c4b937` (клас тягача з підтвердженням; «Каса» з поділом компаній), `27ed4f0` (тривога на передплачений рахунок).

**Стан продакшену 25.09 (перевірено читанням):** код `474abcd` = HEAD `ui/journal-redesign` (0 комітів позаду, робоче дерево чисте) — розгорнуто й `9c186d4` (мобільний пакет), `7b35751` (вхід IKK — чекати форму), `474abcd` (звіт e-TOLL — кінцева дата «сьогодні»); alembic `a1e6d8c4b937`, Ledger `0006_audit_actor` — без змін. Сайт перезапущено 24.09 04:51 UTC, `transfin-etoll-stage2` — 23.09 21:20 UTC, recorder — 20.09. `transfin-pgdump`: останній успіх 24.09 03:29 UTC. `/var/backups/transfin` — 202 МБ, з них ~150 МБ ручні дампи `before_*` / `pre-*`.

**PostGIS** (23.09, окремий СТОП, дозвіл власника): пакет `postgresql-18-postgis-3` 3.6.2 з репозиторію Ubuntu, `CREATE EXTENSION postgis` у `transfin_backend`. База 75 → 110 МБ. Колонка `etoll.toll_sections.geom` — **GENERATED ALWAYS ... STORED** з того самого JSON (плоска, `ST_Force2D`), тож код знімка її не оновлює й не може забути. Пам'ять сервера при цьому не змінилась (3,7 ГБ усього, ~2,8 вільно); стежити за нею — на етапі зіставлення треків.

**Читання звіту e-TOLL:** через «Pobierz plik CSV» (один файл замість ~110 розгортань рядків); розгортання лишилось запасним шляхом, вимикач `ETOLL_REPORT_DETAILS`. Пояс браузера пінимо `Europe/Warsaw` — кабінет показує час за поясом того, хто дивиться (сторінка на сервері була в UTC, вивантаження власника в київському; звідси «зайві» три години). Зберігаємо і час кабінету, і момент `occurred_at_utc`; **тотожність проїзду — за моментом**. Ліміти: 6 звітів на годину, той самий період не перепитуємо частіше ніж раз на 6 годин.

**Тривоги e-TOLL без звернень до кабінету:** `low_prepaid_balance` (передплата, автопоповнення вимкнене, баланс < `ETOLL_LOW_PREPAID_PLN`, типово 200 PLN, тягач за кордоном за даними еЧерги) — 23.09 знайшла 4 тягачі; `unpaid_toll` («Kwota nieuiszczona» > 0 у рядку звіту).

**Відкрите питання (власник з'ясовує):** сторінка «Konta rozliczeniowe» показує 19 рахунків, а фільтр рахунку у звіті — лише «Prepay» (106294); пошук за назвою й номером решти не знаходить. Тому звіти читаються лише по тягачах цього рахунку; решті — тривога за балансом. Також назви рахунків не збігаються з тягачами (рахунок «AC8139BO» — 15,09 PLN, а тягач AC8139BO платить з «AC8975EK»); зв'язок беремо з `etoll.ikk_vehicles.account_name`.

### Потік 1 — операційне (нумерація — як у HANDOFF §5)
Поточний порядок (власник, 25.09): бекапи поза сервером → e-TOLL (п. 7) → SENT/RMPD розвідка (п. 12) → чеки (п. 3) і сповіщення — HANDOFF §5.
1. **Журнал.** Відкрите питання: видалення для логістів — лише чернетки (власник, 22.09).
2. **«Каса»** — **етапи 1–4 завершено 22.09** (основа, зв'язок із Журналом, сторінка, звіти з Excel). Таблиці `cash_boxes`, `cash_movements`, `cash_exchanges`, `cash_leg_formalization`, `cash_audit_log`; 8 тригерів захисту (append-only, заборона мінуса, позначка оформлення); `/api/cash/*` — SUPER_ADMIN і FINANCE (своїх компаній), початковий залишок — лише SUPER_ADMIN. **Відкат міграції видаляє історію каси — лише з окремого дозволу власника.** Прибутковість — окремо по валютах, без перерахунку. З 23.09 — поділ компаній ВТ / ТЛ.
3. **Чеки по банківських операціях** — план не складено.
4. **Підключення Wialon** — план не складено; токен — у картці компанії (§7).
5. **CRM** — план не складено; джерело — таблиця розрахунків з клієнтами власника.
6. **Витрати рейсу, прибутковість, аванс, авансовий звіт** — план не складено.
7. **Розрахунок e-TOLL.** План на 4 етапи затверджено 22.09 з рішеннями: повторний запит періоду з сьогоднішнім днем через 6 год, ≤ 6 звітів/год; знімок публічних служб калькулятора (запасний — OSM і ставки з постанови), без обходу перевірки безпеки; PostGIS — окремим СТОП; клас тягача підтверджує лише власник; точки треку під нарахованими ділянками — 12 міс., решта позицій — 30 днів; самоперевірка о 07:00 Київ. Форма звіту IKK («Raporty → Numer rejestracyjny»): дати — кліками в календарі + «Ustaw daty» (Escape стирає дати, Enter згортає фільтри), **кінцева дата ≤ сьогодні** (кабінет дає сьогоднішній день, відмовляє лише майбутньому; `474abcd`), таблиця за ~16 с. Тимчасова розвідка — `/home/transfin/etoll_probe/` (видалити після етапу 1). Позиції Tekson — з 19–20.09; 3 тягачі лише у Wialon.
8. **GPS етап 1** — план не складено; Tekson (`etoll.positions`), Wialon ще не підключено.
9. **єЧерга: листи й «час заїжджати», перевірка реєстрації, сторожова тривога** — план не складено; воркер на Mac (§3).
10. **Панель термінів документів** · 11. **Тахограф і розрахунки з водіями** · 12. **RMPD / SENT-агент** · 13. **GPS етапи 2–5** · 14. **«Зелена картка» наперед** — не почато.

**Відкладено:** система сповіщень (рішення прийнято: варіант C, опитування Telegram, AlphaSMS, N₁ = 10 хв, M = 6, N₂ = 15 хв, N₃ = 30 хв, K = 4 год, тиша 22–07, сторінка налаштувань) — HANDOFF §5.

### Потік 2 — удосконалення
- Роль «Перегляд» — перевірка колегою (personnel id 3: FINANCE + VIEWER, ВОТРАНС).
- Логісти й зміна пароля (зараз паролі задає власник).
- Резервні копії поза сервером: **на сервері нічого не налаштовано — restic, borg, rclone не встановлено** (перевірено 25.09). План (HANDOFF §5) погоджено 25.09, чекає на акаунти: restic → Storage Box HEL + B2 EU, зберігання 14/8/усі місячні; третя копія (дзеркало iCloud) — не вирішено. Версію restic записати сюди після встановлення. Вкладення відрізків (§8) і Mac (`worker.env`, pgpass, ключ тунелю) — у складі плану.
- Журнал: статуси замовлень, аналітика, e-CMR.
- Довідники: дублікати, історія призначень, ЄДРПОУ / NIP, відповідальні.
- Фінансовий модуль (колега); Ledger на проді порожній (0 документів, 0 рахунків, 22.09).
- Мобільний інтерфейс.
- Документи з пошти (окрема скринька для листів e-TOLL — §7), бот водіїв.
- Решта даних з Mac (§7).
- З дослідження: біржі, календар парку, KPI, портал клієнтів, ризики контрагентів, ремонти й склад, штрафи й ДТП, дозволи ЄКМТ.

**Відкладено до потреби:** Mac mini з ДБЖ.
